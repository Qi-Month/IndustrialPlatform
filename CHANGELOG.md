# Industrial Platform 2.0.0 更新日志

> **一句话概括**: 这次更新把平台从"改配置文件 + 手持扳手直接展开"改成了"右键开界面, 自己选、自己看、自己搭", 同时还多了一个专门的平台设计台, 平台外观重新变成可以用数据包覆盖的结构文件。

---

## 一、开场速览(30 秒版)

- **右键就能开界面了**: 空手右键平台方块就能打开搭建界面, 不用再特意去翻扳手或者木棍
- **看得见才会搭**: 界面里选模式、拉填充格数, 外面那个蓝色预览会**实时跟着变**, 搭之前就知道会盖成什么样
- **新物品 · 填充调节器**: 滚轮直接改上下填充格数, 数值记在物品上, 右键平台直接展开
- **新方块 · 平台设计台**: 把你自己的结构文件丢进 `schematics/platform`, 就能在游戏里直接搭出来
- **数据包能改平台外观了**: 四种内置平台重新变回结构 NBT, 数据包放同名文件即可覆盖

---

## 二、平台搭建界面(本次最大改动)

### 旧版是怎么玩的

|             | 旧版                                         |
| ----------- | -------------------------------------------- |
| 打开方式    | 手持扳手/木棍右键, 直接按配置展开, 没有界面  |
| 改填充格数  | 只能去改配置文件, 然后重启游戏               |
| 换样式/尺寸 | 扳手右键循环切换方块状态, 看不见切换到哪个了 |
| 放什么材料  | 配置文件里写死的一种材料                     |
| 会不会盖错  | 完全靠经验, 展开之前看不到范围               |

### 现在是怎么玩的

**怎么打开**: 空手右键平台方块, 或者手持扳手/木棍右键, 都会打开搭建界面(空手只认主手, 避免主手拿着东西时被副手的空手顶开界面)。

**界面里有什么**:

1. **模式四选一** —— 工业 / 棋盘格 × 轻型(1×1 区块) / 重型(3×3 区块), 选中的那个按钮会保持高亮, 鼠标悬停有说明
2. **向上填充格数** —— 数字输入框 + `-` / `+` 按钮 + **鼠标滚轮**(按住 Shift 滚一次 10 格), 范围 0 ~ 64
3. **向下填充格数** —— 同上
4. **材料槽** —— 只能放一个搭建材料(默认任意石头)
5. **搭建按钮** —— 槽里没材料 / 材料不够时按钮是**灰的**, 鼠标移上去会提示"请先在槽位中放入材料", 材料够的时候按钮上会直接写出这次要消耗多少个

**几个体验上的细节**:

- **材料会还给你**: 关掉界面时, 还留在槽位里的材料自动退回背包, 不会卡在界面里
- **每个平台方块自己记设置**: 你在某个方块上选好的模式、上下填充格数会写进存档, 关界面、走开、退出重进游戏再回来, 还是上次那套设置
- **实时预览**: 界面打开时, 正在编辑的那个平台外面会出现
  - 一个**原版碰撞箱样式的线框立方体**, 把这次会被处理到的体积整个框住, 底面跟着 1×1 / 3×3 切换, 高度跟着上下填充格数走, 线框还有轻微的呼吸效果
  - **满建筑高度的波浪外墙**, 标出区块范围(每个区块段的高度不同, 像水波一样上下流动)
  - 一层**半透明平面**, 标出平台方块自己会铺在哪一层
  - 你在界面里改任何一个数字, 外面立刻跟着变, 不用关界面再看
- **界面打开时世界会变暗**, 蓝色预览和界面看得更清楚
- **高亮按钮用原版贴图**, 不再是一层橙色滤镜糊上去

**关掉界面回到老玩法**: 配置项 `enable_builder_ui` 设成 `false`, 界面就整个关掉了, 恢复成以前那套玩法 —— 扳手右键切平台类型, 潜行 + 扳手右键切悬浮, 手持材料右键直接展开(填充格数取配置默认值)。默认是 `true`, 也就是默认走新界面。

---

## 三、新物品: 填充调节器(Fill Adjuster)

一个专门用来调"上下填充格数"的手持工具, 解决的是"不想开界面, 只想改两个数字"的场景。

- **滚轮直接调**: 站着滚滚轮改**向上填充**, 潜行滚滚轮改**向下填充**
- **Ctrl 加速**: 按住 Ctrl 滚一次 10 格
- **Alt 放行**: 按住 Alt 滚滚轮**不再拦截**, 恢复成原版切换快捷栏 —— 这个操作会写在物品说明里, 免得有人不知道怎么切物品栏
- **数值存在物品上**: 上下填充格数写在物品自己的 NBT 里, 这件调节器拿到哪个平台旁边都是同一套数值
- **右键即展开**: 手持调节器右键平台方块, 直接按调节器上的数值展开平台, 不经过界面
- **物品说明**: 第一行显示当前的向上填充, 第二行向下填充, 后面跟着滚轮用法、Alt 用法和右键展开说明
- **合成**: 见下方配方表

---

## 四、新方块: 平台设计台(Platform Designer)

如果说原来的平台是"四款固定款式", 那设计台就是"你自己带图纸来"。

**怎么用**:

1. 把结构文件(`.nbt`, 也就是结构方块导出的那种)丢进 **游戏目录/`schematics/platform`** 文件夹
2. 空手右键(或手持调节器右键)**平台设计台**, 打开设计界面 —— 这个界面**不受 `enable_builder_ui` 开关影响**, 永远可用
3. 在左侧列表里选一张蓝图(显示名字和尺寸, 列表长了可以用滚动按钮/翻页按钮翻)
4. 调上下填充格数(和搭建界面一样的数字框 + 加减 + 滚轮)
5. 看右下角的**材料清单**: 会列出这张蓝图需要哪些方块、各需要多少个
6. 材料够了就点搭建

**材料从哪来**: 界面会扫描你的**主背包和快捷栏**, 以及**半径 4 格、上下各 1 格范围内所有容器**(箱子、桶……)里的物品。材料清单显示的是**真实拥有的数量**, 不够会标出来, 点了也会提示还差多少。**创造模式不消耗材料**。

**缺方块的蓝图直接不显示**: 如果某个结构用到了当前整合包里没有的方块(比如没装那个模组), 这张蓝图**不会出现在列表里**, 而不是让你搭到一半发现少方块。

**文件改动即时生效**: 蓝图缓存按"文件修改时间 + 文件大小"失效, 你把文件丢进去或者覆盖掉, 下次打开界面就是新的, 不用重启游戏。

---

## 五、平台外观重新变成结构文件(数据包可以覆盖了)

这一条是这次的重点之一。

**中间那版的问题**: 有一段时间, 四种内置平台的外观是**代码里算出来的**(边框、灯、棋盘格全写在算法里), 好处是尺寸随便改, 坏处是**数据包完全没法插手** —— 想换个配色、换套方块, 只能改模组本身。

**现在**: 内置平台的外观重新回到**结构 NBT** 上(和更早的版本一样), 放在:

```
data/industrial_platform/structures/industrial.nbt        工业 · 轻型   16×16
data/industrial_platform/structures/industrial_h.nbt      工业 · 重型   48×48
data/industrial_platform/structures/checkerboard.nbt      棋盘格 · 轻型 16×16
data/industrial_platform/structures/checkerboard_h.nbt    棋盘格 · 重型 48×48
```

**数据包怎么覆盖**: 在数据包里放一个**同名同路径**的文件就行, 例如 `你的数据包/data/industrial_platform/structures/industrial.nbt`, 它会直接顶掉模组自带的那个。用 KubeJS 的话就是 `kubejs/data/industrial_platform/structures/industrial.nbt`。这是服务端资源, 所以放在服务端数据包即可。

**结构没了也不会坏**: 如果结构文件被删掉或者改坏了, 模组会**自动回退到内置的程序化生成**(算法算出来的图案和原版结构完全一致), 只是这时不受数据包控制, 不会出现"平台搭不出来"的情况。

**摆放位置不变**: 16×16 的平台, 控制器所在区块就是它自己; 48×48 的平台, 控制器所在区块仍然是正中间那个区块(往西/北各偏一个区块), 和结构 NBT 年代完全一致。

---

## 六、配方

| 物品           | 形状                  | 材料                                                                     |
| -------------- | --------------------- | ------------------------------------------------------------------------ |
| **平台设计台** | `ABA` / `BCB` / `DDD` | A = 任意染料 ×2, B = 深板岩类 ×3, C = **工业平台方块** ×1, D = 石头类 ×3 |
| **填充调节器** | `A A` / `ABA` / ` C ` | A = 铁锭 ×5, B = 石头类 ×1, C = 深板岩类 ×1                              |

两个配方都带进度解锁条件(拿到平台方块 / 拿到铁锭), 会正常出现在配方书里。

---

## 七、语言

- 补齐了 **日语(ja_jp)**、**文言(lzh)**、**俄语(ru_ru)** 三个语言文件的全部条目, 现在和英文/简中完全对齐(56 条)
- 顺手清掉了两条早就没人用的旧条目(蓝图分页指示器、蓝图提示语), 五个语言文件一起清

---

## 八、配置项一览

| 配置项                                                               | 默认值                                   | 说明                                                                       |
| -------------------------------------------------------------------- | ---------------------------------------- | -------------------------------------------------------------------------- |
| `enable_builder_ui`                                                  | `true`                                   | 关掉就回到老玩法(扳手切模式、潜行切悬浮、材料右键直接展开)                 |
| `top_filling_distance` / `bottom_filling_distance`                   | `5`                                      | 界面里上下填充格数的初始值, 玩家仍可在界面里改                             |
| `platform_material`                                                  | `#forge:stone`, `minecraft:cobblestone`  | 能放进材料槽的物品(也接受物品标签 `industrial_platform:platform_material`) |
| `adjuster`                                                           | `#forge:tools/wrench`, `minecraft:stick` | 能打开界面的手持物(现在空手也可以)                                         |
| `cleanup_drops`                                                      | `false`                                  | 向上清理时被顶掉的方块掉不掉落                                             |
| `min_platform_chunks` / `max_platform_chunks`                        | `1` / `4`                                | 程序化生成时的占地范围限制(每个方向 1~8 区块)                              |
| `light_platform_cost` / `heavy_platform_cost` / `filling_layer_cost` | `1` / `1` / `0`                          | 搭建一次消耗的材料数量                                                     |

---

## 九、和旧版对照表

| 项目         | 旧版                                              | 2.0                                                      |
| ------------ | ------------------------------------------------- | -------------------------------------------------------- |
| 打开方式     | 手持扳手/木棍右键直接展开                         | 空手或手持调节器右键**打开界面**                         |
| 预览         | 手持扳手/木棍靠近时画个范围, 界面里看不到变化     | 界面里**实时预览**, 改一个数字外面立刻变                 |
| 改填充格数   | 改配置文件 + 重启                                 | 界面里数字框 / 加减按钮 / 滚轮, 或用填充调节器滚轮       |
| 改样式尺寸   | 扳手循环方块状态                                  | 界面里四选一, 高亮显示当前选择                           |
| 材料         | 手持材料右键, 固定消耗 1 个, 认哪些材料由配置决定 | 材料槽 + 按数量消耗, 不够按钮就是灰的, 关界面自动退回    |
| 设置保存     | 存在方块状态里                                    | 每个方块独立存进存档, 重进游戏不丢                       |
| 自定义平台   | 无                                                | **平台设计台** + 导入 `schematics/platform` 里的结构蓝图 |
| 平台外观来源 | 结构 NBT                                          | 结构 NBT(**数据包可覆盖**), 缺失时回退程序化生成         |
| 界面观感     | 无界面                                            | 打开界面世界变暗, 高亮按钮用原版贴图                     |
| 语言         | 只有中英                                          | 中 / 英 / 日 / 文言 / 俄                                 |

---

## 十、技术实现(这部分是给写代码的人看的)

### 结构系统

- 平台搭建走 `StructureTemplateManager.get(ResourceLocation)` 读 `data/<命名空间>/structures/<id>.nbt`, 所以**数据包同名文件即覆盖**
- 放置用 `StructureTemplate.placeInWorld` + 自定义 `StructureProcessor`: 放之前先检查目标位置, 不可破坏方块(硬度 < 0, 比如基岩)直接跳过, 其余方块按 `cleanup_drops` 决定掉不掉落, 模板里的空气格会把原方块清掉
- 摆放锚点 `originOf(x, size)`: `floorDiv(x,16)*16 - ((ceil(size/16)-1)/2)*16`, 16 格不偏移, 48 格往西/北偏一个区块
- 向上填充从 `originY + 结构高度` 起算(内置结构 1 层高, 所以等价于原来的 `posY + 1`), 向下填充范围按结构实际 XY 尺寸算

### 程序化生成器(兜底)

- `PlatformGenerator`: 边框 `(x+z) % 2 == 0 ? 主色 : 副色`, 灯在内缩 1 格的环上、间隔 15 格, 棋盘样式 `(x+z) % 2 == 1 ? 底色 : 副色`
- 材质统计与老 NBT 逐项一致(工业 30/30/4/192, 棋盘格 30/30/4/96/96), 所以回退生成出来的外观和结构文件是一样的

### 蓝图解析

- `PlatformBlueprint` 直接读原版结构格式: `size` / `palette`(或 `palettes` 取第一组) / `blocks`, 用 `BuiltInRegistries.BLOCK` 查表, 跳过 `air` 与 `structure_void`
- 查不到的方块记进 `missingBlocks`, 该蓝图就从列表里隐藏(`BlueprintLibrary.list()`), 保证"能选到的蓝图一定搭得出来"
- `BlueprintLibrary` 扫描 `FMLPaths.GAMEDIR/schematics/platform`, 缓存按"修改时间 + 大小"失效

### 材料扫描

- 来源 = 主背包与快捷栏(`RangedWrapper(new InvWrapper(player.getInventory()), 0, 36)`, 不碰盔甲和副手) + 半径 4 格、y−1 ~ y+1 范围内所有 `Container`
- 提供 `count / canAfford / missing / consume / give`, 扣材料时先扣材料槽再扣背包, 不够的部分原路退回

### 界面与同步

- 两个界面都是 `AbstractContainerMenu` + `SimpleContainerData`(`addDataSlots`), 通过 `NetworkHooks.openScreen(...)` + `SimpleMenuProvider` 打开, 方块坐标跟着一起发过去
- 自定义网络通道 `SimpleChannel`, C2S/S2C 包统一用静态 `encode / decode / handle`, 处理器跑在 `consumerMainThread`
- 界面里的改动会立刻落盘并改方块状态, 同时把设置同步给客户端(`PlatformSettingsSyncPacket`), 这样**关掉界面之后**, 世界里的区块边界预览依然知道每个平台的模式和填充格数

### 数据持久化

- 每个平台方块的设置存在 `SavedData`(`PlatformSettingsStorage`, 存档里 key 为 `industrial_platform_settings`), 方块被挖掉/被结构覆盖时在 `onRemove` 里顺手清掉
- 客户端另有一份 `PlatformPreviewSettings` 缓存, 服务端每 5 tick 只把**变化过**的设置推给附近玩家

### 渲染

- 预览在 `RenderLevelStageEvent.Stage.AFTER_TRANSLUCENT_BLOCKS` 阶段绘制, 整体只用一种蓝色
- 波浪外墙: 手写 `QUADS` 顶点, 满建筑高度、每 4 格一段, 每段的 alpha 按 `sin(高度 * 频率 − 时间)` 起伏
- 填充范围线框: 走原版 `LevelRenderer.renderLineBox` + `RenderType.lines()`, 带一个低频呼吸的 alpha
- 甲板层: 抬高 0.01 格避免和方块面 z-fighting 的半透明面
- 界面打开时正在编辑的那个平台单独画, 和后台扫描出来的结果去重

### 其它

- 英文/简中的语言文件由 `runData` 数据生成产出, 日/文言/俄为手写文件, 五份 key 完全对齐
- 顺手整理过包结构(平台相关类收进 `common/`), 并清掉了几百处多余的 `this.`
- 兼容性: JEI 依赖提到 `15.20.0.106`(旧版本会让 Create 的 ModSorter 直接报错), Create 依赖 `6.0.8-289`

### 验证情况(重要)

这次的改动只做到了**编译通过 + 数据生成通过 + 资源打包检查通过**(`compileJava` / `runData` / `processResources`), **没有实际启动客户端进游戏实测**。界面坐标、预览位置、结构摆放这些最终效果建议进游戏过一遍再录视频。

---

## 十一、这次没做的 / 可以继续做的

- 搭建成本目前仍按配置里的固定数量算(`light/heavy_platform_cost`), 换了个几百格的大结构, 成本不会跟着变 —— 可以改成按结构实际方块数计算
- 向下填充目前固定垫**石头**, 还没做成可配置的填充方块
- `cleanup_drops`(清理掉落)暂时只能在配置文件里改, 界面里还没有开关
- 被数据包删掉的结构文件只会在日志里提示, 界面上暂时看不到"哪几个款式不可用"
